projects:
  - name: '<script>alert('1')</script>' # Name may contain lowercase letters, spaces, numbers, hyphens, and underscores
    description: 'Your project description'

    # List of totally customizable environments, e.g Test, Stage, QA, CI-CD
    environments:
      - name: '<script>alert('1')</script>' # Name may contain letters, spaces, numbers, hyphens, and underscores
        slug: '<script>alert('1')</script>' # Slug may contain letters, numbers, hyphens, and underscores
        configs:
          - slug: 'dev' # First slug *must* match environment slug name

      - name: 'CI-CD'
        slug: 'ci-cd'
        configs:
          - slug: 'ci-cd'
          - slug: '<script>alert('1')</script>' # Branch configs supported
          - slug: 'ci-cd_gitlab'

      - name: '<script>alert('1')</script>'
        slug: '<script>alert('1')</script>'
        configs:
          - slug: 'test'

      - name: '<script>alert('1')</script>'
        slug: '<script>alert('1')</script>'
        configs:
          - slug: 'prd'

    # Define secrets for each environment
    secrets:
      dev:
        ENV: 'development'
        API_KEY: ''

      ci-cd:
        DOCKER_HUB_USERNAME: ''
        DOCKER_HUB_TOKEN: ''

      test:
        ENV: 'development'
        API_KEY: ''

      prd:
        ENV: 'production'
        API_KEY: ''
